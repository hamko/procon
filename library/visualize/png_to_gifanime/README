g++ main.cpp mypng.cpp -lpng
./a.out

cd sort
#サイズを大きくして
ls *.png | parallel -j 10 convert {} -resize 300% resized_{}
#gifアニメに
ls resized_* | sort | awk '{if (NR%3==0)print}' > tmp;  convert -delay 1 -loop 0 `cat tmp` movie.gif
